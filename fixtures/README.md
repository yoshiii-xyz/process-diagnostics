# Fixtures

The integration tests use explicit system commands and temporary transcript
paths so the repository contains no executable fixture scripts or private
environment values. Keep any committed fixture metadata synthetic.
